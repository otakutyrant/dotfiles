backend = "glx";
vsync = true;
use-damage = true;

// Let fullscreen windows bypass the compositor so games can present at the
// monitor's native refresh instead of being throttled by desktop composition.
unredir-if-possible = true;

detect-client-opacity = true;
detect-rounded-corners = true;
